#!/usr/bin/make -f
DH_VERBOSE = 1

DPKG_EXPORT_BUILDFLAGS = 1
include /usr/share/dpkg/default.mk

%:
	dh $@ 

override_dh_auto_test:
	# testing is broken now

override_dh_install:
	dh_install
	rm -r $(CURDIR)/debian/cmake-fedora/usr/bin

override_dh_installchangelogs:
	dh_installchangelogs --keep
